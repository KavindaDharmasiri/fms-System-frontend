import { MaskAcquirerBinPipe } from './mask-acquirer-bin.pipe';

describe('MaskAcquirerBinPipe', () => {
  it('create an instance', () => {
    const pipe = new MaskAcquirerBinPipe();
    expect(pipe).toBeTruthy();
  });
});
